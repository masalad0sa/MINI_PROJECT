fastapi
uvicorn
python-multipart
opencv-python
mediapipe
numpy
ultralytics
# Optional pretrained gaze backend (hybrid mode falls back safely if unavailable):
# Install with: pip install l2cs-net torch torchvision
# l2cs-net
# torch
# torchvision
# Optional shadow ML scoring (legacy miniproject-inspired):
# pandas
# scikit-learn
# joblib
# protobuf is managed by mediapipe/ultralytics, avoiding manual pin to prevent conflicts
